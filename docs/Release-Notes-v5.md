# v5 changes

- Header now shows a "Logged in as ..." banner with email/name and role.
- Added Admin → Users page at `/admin/users` for easy role assignment.
- Added SQL snippet `docs/first-user-admin.sql` to make the first signup automatically become admin.
