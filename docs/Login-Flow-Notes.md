# Updated login flow

Home page shows 3 login options:
- /login/food-truck -> redirects to /truck/dashboard
- /login/business -> redirects to /business/dashboard
- /login/admin -> redirects to /admin

The generic /login page is a chooser.
